import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
	height: 60rem;
	width: 30rem;
`;

const Springboard = () => {
	return <h1>Springboard</h1>;
};

export default Springboard;
